services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: transit_alert
      POSTGRES_USER: transit
      POSTGRES_PASSWORD: transit
    ports:
      - "5432:5432"
    volumes:
      - transit_pg_data:/var/lib/postgresql/data

volumes:
  transit_pg_data:
